/*const Discord = require("discord.js");

module.exports.run = async (bot, message, args) => {
  if (!message.member.hasPermission("BAN_MEMBERS")) return message.channel.send("You don't have permission, faggot");
  var whitey = message.guild.roles.get("418222331958394880");
  var whiteyList = message.guild.roles.get('418222331958394880').members.map(m=>m);
  var len = whiteyList.length;
  //console.log(whiteyList);
  for(var i = 0; i<len; i++){
    var member = whiteyList[i];
    member.setNickname('');
  }
}

module.exports.help = {
  name: "resetnicks"
}*/
